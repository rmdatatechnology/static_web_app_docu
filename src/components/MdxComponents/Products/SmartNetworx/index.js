import React from "react"
import Product from "../index"

const SmartNetworx = ({ children }) => {
  return <Product currentproduct="smartnetworx" children={children} />
}

export default SmartNetworx
