import React from "react"
import Product from "../index"

const GeoMapperOnly = ({ children}) => {
  return <Product currentproduct="geomapperonly" children={children} />
}

export default GeoMapperOnly
