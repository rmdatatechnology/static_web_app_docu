import React from "react"

import "../../../styles/styles.scss";

const Bold = ({ children }) => {
  return (
    <span className="custom-bold">
      {children}
    </span>
  )
}

export default Bold
