import React from "react"
import TextBlock from "../index"

const Success = ({ children, padding, symbol }) => {
  return <TextBlock theme="success" children={children} padding={padding} symbol=""/>
}

export default Success
